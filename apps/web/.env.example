# Backend (bbgo) HTTP base. bbgo listens on :8080 by default.
# The legacy `/api/v1/*` paths still come from the old custom backend; the
# bbgo-driven API surface evolves under `/api/*` and a backwards-compat shim
# is expected to be introduced in pkg/futu/server.
VITE_API_BASE_URL=http://localhost:8080
