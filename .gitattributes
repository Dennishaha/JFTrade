docs/reference/generated/*.md text eol=lf
# Tauri desktop sources and checked-in contract fixtures should keep checkout
# output identical on Windows.
apps/desktop/src-tauri/** text eol=lf

# The OSS gate verifies the official AGPL text byte-for-byte.
LICENSE text eol=lf

# Repository verification hashes the checked-in Futu protocol inputs and
# generated outputs byte-for-byte. Keep them LF-stable on Windows as well.
pkg/futu/proto/** text eol=lf
pkg/futu/pb/** text eol=lf
